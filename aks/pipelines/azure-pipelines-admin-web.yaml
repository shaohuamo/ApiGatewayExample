# Docker
# Build and push an image to Azure Container Registry, then deploy to AKS
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
  branches:
    include:
      - dev
      - qa
      - uat
      - staging
      - prod
  paths:
    include:
    - src/frontend/admin-web/**

resources:
- repo: self

variables:
  # Reference global variable group for Docker registry and image configuration
  # This group is accessible to Build stage
  - group: admin-web-global

  # Path to the admin-web Dockerfile (Next.js build is handled inside the Dockerfile)
  - name: dockerfilePath
    value: 'src/frontend/admin-web/Dockerfile'
  - name: tag
    value: '$(Build.BuildId)'

  # Agent VM image name
  - name: vmImageName
    value: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        # Build context is the admin-web directory where package.json is located
        buildContext: 'src/frontend/admin-web'
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)

- stage: DeployToDev
  displayName: Deploy to Dev
  dependsOn: Build
  condition: and(succeeded('Build'), eq(variables['Build.SourceBranch'], 'refs/heads/dev'))
  jobs:
  - deployment: DeploymentToDev
    displayName: Deployment to Dev Environment
    pool:
      vmImage: $(vmImageName)
    environment: dev
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
            displayName: Checkout source code

          - task: KubernetesManifest@1
            displayName: Deploy admin-web to dev namespace in kubernetes
            inputs:
              action: deploy
              connectionType: kubernetesServiceConnection
              kubernetesServiceConnection: dev-microservicesdemo-aks-cluster-dev-1778160743494
              namespace: dev
              manifests: aks/manifests/dev/frontend/admin-web-dev.yaml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)

- stage: DeployToQA
  displayName: Deploy to QA
  dependsOn: Build
  condition: and(succeeded('Build'), eq(variables['Build.SourceBranch'], 'refs/heads/qa'))
  jobs:
  - deployment: DeploymentToQA
    displayName: Deployment to QA Environment
    pool:
      vmImage: $(vmImageName)
    environment: qa
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
            displayName: Checkout source code

          - task: KubernetesManifest@1
            displayName: Deploy admin-web to qa namespace in kubernetes
            inputs:
              action: deploy
              connectionType: kubernetesServiceConnection
              kubernetesServiceConnection: qa-microservicesdemo-aks-cluster-qa-1778160953610
              namespace: qa
              manifests: aks/manifests/qa/frontend/admin-web-qa.yaml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)

- stage: DeployToUAT
  displayName: Deploy to UAT
  dependsOn: Build
  condition: and(succeeded('Build'), eq(variables['Build.SourceBranch'], 'refs/heads/uat'))
  jobs:
  - deployment: DeploymentToUAT
    displayName: Deployment to UAT Environment
    pool:
      vmImage: $(vmImageName)
    environment: uat
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
            displayName: Checkout source code

          - task: KubernetesManifest@1
            displayName: Deploy admin-web to uat namespace in kubernetes
            inputs:
              action: deploy
              connectionType: kubernetesServiceConnection
              kubernetesServiceConnection: uat-microservicesdemo-aks-cluster-uat-1778161095509
              namespace: uat
              manifests: aks/manifests/uat/frontend/admin-web-uat.yaml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)

- stage: DeployToStaging
  displayName: Deploy to Staging
  dependsOn: Build
  condition: and(succeeded('Build'), eq(variables['Build.SourceBranch'], 'refs/heads/staging'))
  jobs:
  - deployment: DeploymentToStaging
    displayName: Deployment to Staging Environment
    pool:
      vmImage: $(vmImageName)
    environment: staging
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
            displayName: Checkout source code

          - task: KubernetesManifest@1
            displayName: Deploy admin-web to staging namespace in kubernetes
            inputs:
              action: deploy
              connectionType: kubernetesServiceConnection
              kubernetesServiceConnection: staging-microservicesdemo-aks-cluster-staging-1778161154539
              namespace: staging
              manifests: aks/manifests/staging/frontend/admin-web-staging.yaml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)

- stage: DeployToProduction
  displayName: Deploy to Production
  dependsOn: Build
  condition: and(succeeded('Build'), eq(variables['Build.SourceBranch'], 'refs/heads/prod'))
  jobs:
  - deployment: DeployToProduction
    displayName: Deployment to Production Environment
    pool:
      vmImage: $(vmImageName)
    environment: prod
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
            displayName: Checkout source code

          - task: KubernetesManifest@1
            displayName: Deploy admin-web to prod namespace in kubernetes
            inputs:
              action: deploy
              connectionType: kubernetesServiceConnection
              kubernetesServiceConnection: prod-microservicesdemo-aks-cluster-prod-1778161221255
              namespace: prod
              manifests: aks/manifests/prod/frontend/admin-web-prod.yaml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)
