apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-web-deployment
  labels:
    app: admin-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-web
  template:
    metadata:
      labels:
        app: admin-web
    spec:
      containers:
      - name: admin-web
        image: microservicesdemoregistry.azurecr.io/admin-web:$(tag)
        ports:
        - containerPort: 3000
        env:
        - name: API_GATEWAY_INTERNAL_URL
          value: "http://apigateway-service:8080"
        - name: OTEL_COLLECTOR_INTERNAL_URL
          value: "http://otel-collector:4318"
        - name: APP_ENV
          value: "Production"
        - name: FRONTEND_PUBLIC_URL
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: frontend-public-url
        - name: AUTH_URL
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: frontend-public-url
        - name: NEXTAUTH_URL
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: frontend-public-url
        - name: IDENTITYSERVER_PUBLIC_URL
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: identityserver-public-url
        - name: IDENTITYSERVER_INTERNAL_URL
          value: "http://identityserver-service:8080"
        - name: IDENTITYSERVER_FRONTEND_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: identityserver-frontend-client-id
        - name: IDENTITYSERVER_FRONTEND_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: identityserver-frontend-client-secret
        - name: AUTH_SECRET
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: auth-secret
        - name: AUTH_POSTGRES_HOST
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: postgres-host
        - name: AUTH_POSTGRES_PORT
          value: "5432"
        - name: AUTH_POSTGRES_DATABASE
          value: "adminwebdatabase"
        - name: AUTH_POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: postgres-user
        - name: AUTH_POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: postgres-password
        - name: AUTH_REDIS_URL
          valueFrom:
            secretKeyRef:
              name: admin-web-secrets
              key: auth-redis-url
        - name: AUTH_ACCESS_TOKEN_DENYLIST_PREFIX
          value: "admin-web:access-token-denylist"
        readinessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 15
        resources:
          requests:
            cpu: 10m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: admin-web-service
  labels:
    app: admin-web
spec:
  selector:
    app: admin-web
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
