apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservices-ingress-staging
  namespace: staging
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  tls:
    - hosts:
        - apigateway.staging.example.com
        - admin.staging.example.com
      secretName: microservices-tls-staging
  rules:
    # API Gateway - entry point for all backend API traffic
    - host: apigateway.staging.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: apigateway-service
                port:
                  number: 8080

    # Admin Web - frontend application
    - host: admin.staging.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin-web-service
                port:
                  number: 3000

    # NOTE: products-microservice and test-microservice are intentionally NOT exposed
    # externally. All backend API traffic must go through the API Gateway.
