services:
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.100.0
    container_name: otel-collector-container
    hostname: otel-collector
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ../../configs/otel-collector-config.yml:/etc/otelcol/config.yaml
    ports:
      - "4317:4317"  # OTLP gRPC
      - "4318:4318"  # OTLP HTTP
      - "8889:8889"  # Prometheus exporter metrics
    networks:
      - app-network
    depends_on:
      - jaeger
      - loki
  jaeger:
    image: jaegertracing/all-in-one:1.54
    container_name: jaeger-container
    hostname: jaeger
    environment:
      - METRICS_STORAGE_TYPE=prometheus
      - PROMETHEUS_SERVER_URL=http://prometheus:9090
      - PROMETHEUS_QUERY_SUPPORT_SPANMETRICS_CONNECTOR=true
      - PROMETHEUS_QUERY_NORMALIZE_CALLS=true
      - PROMETHEUS_QUERY_NORMALIZE_DURATION=true
    ports:
      - "16686:16686"   # UI
    networks:
      - app-network
  loki:
    image: grafana/loki:2.9.3
    container_name: loki-container
    hostname: loki
    volumes:
      - loki_storage:/loki
    ports:
      - "13100:3100"
    networks:
      - app-network
  promtail:
    image: grafana/promtail:2.9.0
    container_name: promtail-container
    volumes:
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - ../../configs/promtail/config.yml:/etc/promtail/config.yml
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: -config.file=/etc/promtail/config.yml
    networks:
      - app-network
    depends_on:
      - loki
  prometheus:
    image: prom/prometheus:v3.1.0
    container_name: prometheus-container
    hostname: prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--enable-feature=exemplar-storage,otlp-receiver'
      - '--web.enable-otlp-receiver'
    volumes:
      - ../../configs/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ../../configs/prometheus/rules:/etc/prometheus/rules
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    depends_on:
      - otel-collector
    networks:
      - app-network
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: alertmanager-container
    hostname: alertmanager
    ports:
      - 9093:9093
    volumes:
      - ../../configs/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - ../../configs/secrets/alertmanager-slack-webhook.txt:/etc/alertmanager/secrets/slack_webhook_url:ro
    networks:
      - app-network
  grafana:
    image: grafana/grafana:10.3.3
    container_name: grafana-container
    hostname: grafana
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
    volumes:
      - ../../configs/grafana/datasource.yml:/etc/grafana/provisioning/datasources/datasource.yaml
      - ../../configs/grafana/dashboards/dashboard.yml:/etc/grafana/provisioning/dashboards/dashboard.yml
      - ../../configs/grafana/dashboards:/etc/grafana/provisioning/dashboards
    ports:
      - "13000:3000"
    depends_on:
      - prometheus
      - loki
      - jaeger
    networks:
      - app-network
  redis:
    image: redis:latest
    container_name: redis-container
    hostname: redis
    command: redis-server --maxmemory 2gb --maxmemory-policy allkeys-lru
    ports:
     - 6379:6379
    healthcheck:
     test: ["CMD", "redis-cli", "ping"]
     interval: 10s
     timeout: 5s
     retries: 5
     start_period: 10s
    networks:
     - app-network
  redis-exporter:
      image: oliver006/redis_exporter:v1.58.0
      container_name: redis-exporter-container
      hostname: redis-exporter
      environment:
        - REDIS_ADDR=redis:6379
      ports:
        - "9121:9121"
      depends_on:
        - redis
      networks:
        - app-network
  rabbitmq:
    image: rabbitmq:4.2-management
    container_name: rabbitmq-container
    hostname: rabbitmq
    environment:
     - RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS=-rabbitmq_prometheus return_per_object_metrics true
    volumes:
     - rabbitmq_data:/var/lib/rabbitmq
     - ../../configs/rabbitmq/enabled_plugins:/etc/rabbitmq/enabled_plugins #enable prometheus plugin(expose metric endpoint)
    ports:
     - 5672:5672
     - 15672:15672
     - 15692:15692   # Prometheus metrics
    healthcheck:
     test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
     interval: 10s
     timeout: 10s
     retries: 10
     start_period: 60s
    networks:
     - app-network
  postgres:
   image: postgres:16-alpine
   container_name: postgres-container
   hostname: postgres
   environment:
    - POSTGRES_USER=postgres
    - POSTGRES_PASSWORD=admin
   ports:
    - "5432:5432"
   volumes:
    - ../../configs/postgres/init:/docker-entrypoint-initdb.d
    - ../../configs/postgres/conf/postgresql.conf:/etc/postgresql/postgresql.conf
   command: postgres -c config_file=/etc/postgresql/postgresql.conf
   healthcheck:
    test: ["CMD-SHELL", "test \"$$(cat /proc/1/comm)\" = postgres && pg_isready -U postgres"]
    interval: 10s
    timeout: 5s
    retries: 10
    start_period: 60s
   networks:
    - app-network
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    container_name: postgres-exporter-container
    hostname: postgres-exporter
    environment:
      DATA_SOURCE_NAME: "postgresql://exporter:exporterpass@postgres:5432/productsdatabase?sslmode=disable"
      PG_EXPORTER_EXTEND_QUERY_PATH: /etc/postgres_exporter/queries.yaml
    volumes:
      - ../../configs/postgres-exporter/queries.yaml:/etc/postgres_exporter/queries.yaml
    ports:
      - "9187:9187"
    depends_on:
      - postgres
    networks:
      - app-network
  productsmicroservice.api:
    image: lambdazb/productmicroservice:${PRODUCTS_IMAGE_TAG:-latest}
    container_name: productMicroservice-container
    hostname: productapi
    environment:
     - ASPNETCORE_ENVIRONMENT=Production
     - POSTGRES__HOST=postgres
     - POSTGRES__PORT=5432
     - POSTGRES__DATABASE=productsdatabase
     - POSTGRES__USER=postgres
     - POSTGRES__PASSWORD=admin
     - POSTGRES__MaxRetryCount=5
     - POSTGRES__MaxRetryDelaySeconds=10
     - RabbitMQ__HostName=rabbitmq
     - RabbitMQ__UserName=guest
     - RabbitMQ__Password=guest
     - RabbitMQ__Port=5672
     - RabbitMQ__MaxRetryAttempts=10
     - RabbitMQ__InitialDelaySeconds=2
     - RabbitMQ__MaxDelaySeconds=30
     - RabbitMQ__NetworkRecoveryIntervalSeconds=10
     - RabbitMQ_Products_Exchange=products.exchange
     - RabbitMQ_Products_RoutingKey=products.add
     - ServiceBus__ConnectionString=${SERVICEBUS_CONNECTION_STRING}
     - ServiceBus__ProductsUpdatesTopic=products.updates
     - ServiceBus__ProductsUpdatesSubscription=products.updates.test
     - ServiceBus__ProductsUpdatesReprocessTopic=products.updates.Reprocess
     - Redis__ConnectionString=redis:6379
     - Redis__InstanceName=ProductMicroservice_
     - Redis__ConnectRetry=5
     - Redis__ConnectTimeout=5000
     - Redis__SyncTimeout=5000
     - Redis__MaxReconnectDelay=5000
     - Redis__InitialReconnectDelay=1000
     - Redis__AbortOnConnectFail=false
     - Redis__DelayedDeleteMs=2000
     - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
     - OTEL_RESOURCE_ATTRIBUTES=deployment.environment=production
     - TestMicroserviceName=testapi1
     - TestMicroservicePort=8080
     - CacheOptions__DefaultExpirationMinutes=120
     - CacheOptions__NegativeCacheExpirationMinutes=10
     - UseConsul=true
     - Consul__Host=consul
     - Consul__Port=8500
     - Consul__Discovery__ServiceName=productservice
     - Consul__Discovery__Hostname=productapi
     - Consul__Discovery__Port=8080
    ports:
     - 8581:8080
    networks:
     - app-network
    depends_on:
      rabbitmq:
       condition: service_healthy
      postgres: #service name
       condition: service_healthy
      redis:
       condition: service_healthy
      consul:
       condition: service_healthy
  identityserver:
    image: lambdazb/identityserver:${IDENTITYSERVER_IMAGE_TAG:-latest}
    container_name: identityserver-container
    hostname: identityserver
    environment:
     - ASPNETCORE_ENVIRONMENT=Production
     - POSTGRES__HOST=postgres
     - POSTGRES__PORT=5432
     - POSTGRES__DATABASE=identitydatabase
     - POSTGRES__USER=postgres
     - POSTGRES__PASSWORD=admin
     - POSTGRES__MaxRetryCount=5
     - POSTGRES__MaxRetryDelaySeconds=10
     - IdentityServer__IssuerUri=${IDENTITYSERVER_PUBLIC_URL:-http://localhost:8085}
     - IdentityServer__TokenLifetimes__AccessTokenSeconds=900
     - IdentityServer__TokenLifetimes__AbsoluteRefreshTokenSeconds=2592000
     - ResendEmail__ApiToken=${RESEND_EMAIL_API_TOKEN:-}
     - ResendEmail__From=${RESEND_EMAIL_FROM:-MicroservicesDemo <verify@auth.250669.xyz>}
     - ResendEmail__PublicBaseUrl=${IDENTITYSERVER_PUBLIC_URL:-http://localhost:8085}
     - ResendEmail__ConfirmationSubject=${RESEND_EMAIL_CONFIRMATION_SUBJECT:-Confirm your MicroservicesDemo account}
     - Redis__ConnectionString=redis:6379
     - Redis__InstanceName=IdentityServer_
     - EmailVerificationRateLimit__Enabled=${EMAIL_VERIFICATION_RATE_LIMIT_ENABLED:-true}
     - IdentityServer__SigningCredential__CertificatePath=${IDENTITYSERVER_SIGNING_CERTIFICATE_PATH:?set-identityserver-signing-certificate-path}
     - IdentityServer__SigningCredential__CertificatePassword=${IDENTITYSERVER_SIGNING_CERTIFICATE_PASSWORD:?set-identityserver-signing-certificate-password}
     - SeedUser__Enabled=${IDENTITYSERVER_SEED_USER_ENABLED:-false}
     - SeedUser__UserName=${IDENTITYSERVER_SEED_USER_NAME:-}
     - SeedUser__Email=${IDENTITYSERVER_SEED_USER_EMAIL:-}
     - SeedUser__Password=${IDENTITYSERVER_SEED_USER_PASSWORD:-}
     - Clients__Frontend__PublicUrl=${FRONTEND_PUBLIC_URL:-http://localhost:3000}
     - Clients__Frontend__ClientSecret=${IDENTITYSERVER_FRONTEND_CLIENT_SECRET:-frontend-secret}
     - Clients__Postman__RedirectUri=https://oauth.pstmn.io/v1/callback
     - DataProtection__KeysPath=/app/data-protection-keys
     - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
     - OTEL_RESOURCE_ATTRIBUTES=service.name=identityserver,deployment.environment=production
    ports:
     - 8085:8080
    volumes:
     - ${IDENTITYSERVER_SIGNING_CERTIFICATE_HOST_PATH:?set-identityserver-signing-certificate-host-path}:${IDENTITYSERVER_SIGNING_CERTIFICATE_PATH:?set-identityserver-signing-certificate-path}:ro
     - identityserver_dataprotection:/app/data-protection-keys
    networks:
     - app-network
    healthcheck:
     test: ["CMD-SHELL", "grep -qi ':1F90 .* 0A ' /proc/net/tcp /proc/net/tcp6"]
     interval: 10s
     timeout: 5s
     retries: 10
     start_period: 30s
    depends_on:
      postgres:
       condition: service_healthy
      redis:
       condition: service_healthy
  apigateway:
    image: lambdazb/apigateway:${APIGATEWAY_IMAGE_TAG:-latest}
    container_name: apiGateway-container
    hostname: apigateway
    environment:
    - UseConsul=true
    - Authentication__Authority=${IDENTITYSERVER_PUBLIC_URL:-http://localhost:8085}
    - Authentication__MetadataAddress=http://identityserver:8080/.well-known/openid-configuration
    - Authentication__Audience=gateway-api
    - Authentication__RequireHttpsMetadata=false
    - Authentication__DenylistRedisConnectionString=redis:6379
    - Authentication__AccessTokenDenylistPrefix=admin-web:access-token-denylist
    - Authentication__DenylistFailClosed=true
    - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
    - OTEL_RESOURCE_ATTRIBUTES=service.name=apigateway,deployment.environment=production 
    ports:
     - 9080:8080
    networks:
     - app-network
    depends_on:
     consul: #service name
       condition: service_healthy
     test-microservice1:
       condition: service_started
     productsmicroservice.api:
       condition: service_started
     identityserver:
       condition: service_healthy
     redis:
       condition: service_healthy
  test-microservice1:
    image: lambdazb/testmicroservice:${TESTMICROSERVICE_IMAGE_TAG:-latest}
    container_name: testMicroservice-container-1
    hostname: testapi1
    environment:
     - ASPNETCORE_ENVIRONMENT=Production
     - App_Name=App_A
     - UseConsul=true
     - Consul__Host=consul
     - Consul__Port=8500
     - Consul__Discovery__ServiceName=testservice
     - Consul__Discovery__Hostname=testapi1
     - Consul__Discovery__Port=8080 #test-microservice port inside container
     - RabbitMQ__HostName=rabbitmq
     - RabbitMQ__UserName=guest
     - RabbitMQ__Password=guest
     - RabbitMQ__Port=5672
     - RabbitMQ__MaxRetryAttempts=10
     - RabbitMQ__InitialDelaySeconds=2
     - RabbitMQ__MaxDelaySeconds=30
     - RabbitMQ__NetworkRecoveryIntervalSeconds=10
     - RabbitMQ_Products_Exchange=products.exchange
     - RabbitMQ_Products_RoutingKey=products.add
     - RabbitMQ_Products_Queue=products.add.queue
     - ServiceBus__ConnectionString=${SERVICEBUS_CONNECTION_STRING}
     - ServiceBus__ProductsUpdatesTopic=products.updates
     - ServiceBus__ProductsUpdatesSubscription=products.updates.test
     - ServiceBus__ProductsUpdatesReprocessTopic=products.updates.Reprocess
     - Redis__ConnectionString=redis:6379
     - Redis__InstanceName=TestMicroservice_
     - MessageIdempotency__KeyPrefix=processed-message
     - MessageIdempotency__ProcessedMessageTtlMinutes=1440
     - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
     - OTEL_RESOURCE_ATTRIBUTES=service.name=test.api,deployment.environment=production 
    ports:
     - 8580:8080
    networks:
     - app-network
    depends_on:
     rabbitmq: #service name
      condition: service_healthy
     redis:
      condition: service_healthy
     consul:
      condition: service_healthy
  consul:
    image: hashicorp/consul:latest
    container_name: consul-container
    hostname: consul 
    command: >
      agent -server 
      -bootstrap-expect=1 
      -ui 
      -node=consul 
      -client=0.0.0.0 
      -hcl="disable_update_check = true"
    healthcheck:
     test: ["CMD", "consul", "operator", "raft", "list-peers"]
     interval: 10s
     timeout: 5s
     retries: 5
     start_period: 60s
    ports:
     - 8500:8500
    networks:
     - app-network
  admin-web:
    image: lambdazb/admin-web:${ADMINWEB_IMAGE_TAG:-latest}
    container_name: admin-web-container
    hostname: admin-web
    environment:
      - API_GATEWAY_INTERNAL_URL=http://apigateway:8080
      - OTEL_COLLECTOR_INTERNAL_URL=http://otel-collector:4318
      - FRONTEND_PUBLIC_URL=${FRONTEND_PUBLIC_URL:-http://localhost:3000}
      - AUTH_URL=${FRONTEND_PUBLIC_URL:-http://localhost:3000}
      - NEXTAUTH_URL=${FRONTEND_PUBLIC_URL:-http://localhost:3000}
      - IDENTITYSERVER_PUBLIC_URL=${IDENTITYSERVER_PUBLIC_URL:-http://localhost:8085}
      - IDENTITYSERVER_INTERNAL_URL=http://identityserver:8080
      - IDENTITYSERVER_FRONTEND_CLIENT_ID=test_app
      - IDENTITYSERVER_FRONTEND_CLIENT_SECRET=${IDENTITYSERVER_FRONTEND_CLIENT_SECRET:-frontend-secret}
      - AUTH_SECRET=${AUTH_SECRET:-dev-auth-session-secret-change-me-please}
      - AUTH_POSTGRES_HOST=postgres
      - AUTH_POSTGRES_PORT=5432
      - AUTH_POSTGRES_DATABASE=adminwebdatabase
      - AUTH_POSTGRES_USER=${POSTGRES_USER:-postgres}
      - AUTH_POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-admin}
      - AUTH_REDIS_URL=redis://redis:6379
      - AUTH_ACCESS_TOKEN_DENYLIST_PREFIX=admin-web:access-token-denylist
    ports:
      - 3000:3000
    networks:
      - app-network
    depends_on:
      apigateway:
        condition: service_started
      identityserver:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      otel-collector:
        condition: service_started
networks:
  app-network:
    driver: bridge
volumes:
  rabbitmq_data:
  prometheus_data:
  loki_storage:
  identityserver_dataprotection:
