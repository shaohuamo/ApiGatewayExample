## Azure DevOps MCP Server
- This project uses Azure DevOps. Always check to see if the Azure DevOps MCP server has a tool relevant to the user's request

## Azure CLI Authentication
- Before running any `az` command or use Azure DevOps MCP Server, check authentication status by running `az account show`.
- Only run `az login` if the above command returns an error indicating the user is not logged in.

